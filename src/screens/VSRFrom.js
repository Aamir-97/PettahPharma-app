import React from 'react';
import { Text, ScrollView } from 'react-native';


export default function VSRForm ({navigation}){
    return (
        <ScrollView>
           <Text>This is the Report Form page</Text> 
        </ScrollView>
    )
}